apiVersion: v1
kind: Service
metadata:
  name: {{ include "redpanda.name" . }}-1
spec:
  ports:
    - name: rpc
      protocol: TCP
      port: 33145
      targetPort: 33145
    - name: kafka
      protocol: TCP
      port: 9092
      targetPort: 9092
  selector:
    statefulset.kubernetes.io/pod-name: {{ include "redpanda.name" . }}-1
