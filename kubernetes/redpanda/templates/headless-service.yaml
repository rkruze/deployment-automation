apiVersion: v2
kind: Service
metadata:
  name: redpanda-cluster-kafka-bootstrap
  labels:
    {{- include "redpanda.labels" . | nindent 4 }}
spec:
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redpanda.selectorLabels" . | nindent 4 }}
